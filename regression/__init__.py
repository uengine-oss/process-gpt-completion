"""regression/ — 병합 전 회귀 검증의 **실행**을 맡는다.

## 왜 여기인가

회차 관리(어떤 시나리오를 언제 돌렸고 무엇이 깨졌는지)는 deepagents 의
`resource_eval_*` 가 갖고 있지만, **판정 자체는 여기여야 한다.**

- 프로세스 라우팅의 진실 원천은 폴링 서비스(`polling_service/workitem_processor.py`)다.
  같은 규칙의 사본을 다른 서비스에 두면, 엔진이 합류·분기 규칙을 고칠 때 회귀 테스트만
  옛 규칙으로 통과시킨다 — 회귀 테스트로서 최악이다.
- DMN 규칙 표와 프로세스 정의(`proc_def`, `proc_def_version`)도 이 서비스의 데이터다.

그래서 실행기는 엔진 곁에 두고, deepagents 는 `/regression-run` 으로 위임한다.

## 무엇을 하지 않는가

실행 엔진 인스턴스를 만들지 않고 모델도 부르지 않는다. 저장된 시나리오가 갈림길 판정을
이미 들고 있으므로(생성 시 검증이 `todolist.gateway_decisions` 로 남겼다) 남은 것은
그래프를 따라가는 계산과 규칙 표 조회뿐이다.
"""
